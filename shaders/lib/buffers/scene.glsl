layout(binding = 0) buffer sceneBuffer { // 36
    float Scene_AvgExposure;        // 4
    vec3 Scene_LocalSunDir;         // 16
    vec3 Scene_LocalLightDir;       // 16
    vec3 Scene_SkyIrradianceUp;     // 16

    vec3 Scene_TrackPos;            // 16
};
